type Point {
    x = 0;
    y = 0;

    getX() => self.x;
    getY() => self.y;

    setX(x: Number) => self.x := x;
    setY(y: Number) => self.y := y;
}

let pt = new Point() in
    print("x: " @ pt.getX() @ "; y: " @ pt.setY(4));